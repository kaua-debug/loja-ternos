<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loja de Ternos</title>
    <!-- Carrega o Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Link para o seu arquivo CSS customizado (se houver) -->
    <link rel="stylesheet" href="/style.css">
    <!-- Configuração do Tailwind para usar a fonte Inter -->
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>
<body class="bg-gray-100 text-gray-900">
    <!-- Navbar -->
    <nav class="bg-gray-800 p-4 shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/" class="text-white text-2xl font-bold rounded-md px-3 py-2 hover:bg-gray-700 transition-colors">JCG TERNOS</a>
            <div class="space-x-4">
                <a href="/produtos" class="text-gray-300 hover:text-white rounded-md px-3 py-2 transition-colors">Produtos</a>
                <a href="/carrinho" class="text-gray-300 hover:text-white rounded-md px-3 py-2 transition-colors">Carrinho</a>
                <a href="/login" class="text-gray-300 hover:text-white rounded-md px-3 py-2 transition-colors">Login</a>
                <a href="/admin" class="text-gray-300 hover:text-white rounded-md px-3 py-2 transition-colors">Admin</a>
                <a href="/logout" class="text-gray-300 hover:text-white rounded-md px-3 py-2 transition-colors">Logout</a>
            </div>
        </div>
    </nav>

    <!-- Conteúdo Principal -->
    <main class="container mx-auto p-6 mt-8">
        <h1 class="text-5xl font-extrabold text-center text-gray-800 mb-12 animate-fade-in-down">Bem-vindo à JCG Ternos</h1>

        <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% produtos.forEach(produto => { %>
                <div class="bg-white p-6 rounded-xl shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2">
                    <img src="<%= produto.imagem %>" alt="<%= produto.nome %>" class="w-full h-64 object-cover rounded-lg mb-4 shadow-md">
                    <h2 class="text-2xl font-semibold text-gray-800 mb-2"><%= produto.nome %></h2>
                    <p class="text-gray-600 mb-4"><%= produto.descricao %></p>
                    <p class="text-3xl font-bold text-indigo-600 mb-4">R$ <%= produto.preco.toFixed(2).replace('.', ',') %></p>
                    <a href="/produto/<%= produto.id %>" class="block w-full text-center bg-indigo-600 text-white py-3 px-6 rounded-lg font-semibold hover:bg-indigo-700 transition-colors duration-300 shadow-md hover:shadow-lg">Ver mais</a>
                    <form action="/carrinho" method="POST" class="mt-4">
                        <input type="hidden" name="produtoId" value="<%= produto.id %>">
                        <button type="submit" class="block w-full text-center bg-green-500 text-white py-3 px-6 rounded-lg font-semibold hover:bg-green-600 transition-colors duration-300 shadow-md hover:shadow-lg">Adicionar ao Carrinho</button>
                    </form>
                </div>
            <% }); %>
        </section>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white text-center p-6 mt-12 shadow-inner">
        <div class="container mx-auto">
            <p>&copy; <%= new Date().getFullYear() %> Loja de Ternos. Todos os direitos reservados.</p>
            <div class="flex justify-center space-x-4 mt-2">
                <a href="#" class="hover:text-indigo-400 transition-colors">Privacidade</a>
                <a href="#" class="hover:text-indigo-400 transition-colors">Termos de Serviço</a>
            </div>
        </div>
    </footer>

    <!-- Animação CSS para o título -->
    <style>
        @keyframes fade-in-down {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        .animate-fade-in-down {
            animation: fade-in-down 1s ease-out forwards;
        }
    </style>
</body>
</html>
